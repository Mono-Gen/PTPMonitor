# パッケージング計画 (v1.6.8)

配布用に `v1.6.8` のバイナリおよびドキュメントをZip形式でまとめます。

## 実施内容
1.  **ファイルの集約**:
    - `PTPMonitor.exe` (コンパイル済み)
    - `manual_JP.md`, `manual_EN.md` (最新マニュアル)
    - `config.ini` (設定テンプレート)
    - `README.md` (更新履歴を含むREADME)
2.  **アーカイブ作成**:
    - `PTPMonitor_v1.6.8.zip` として圧縮。

## 検証計画
- Zipファイルが正しく生成され、必要な5つのファイルが含まれていることを確認。
